# helpers.py
# Utility functions (e.g., input validation, formatting)
# Contributors: Add reusable helper functions here
'''

note- it is just a referance code to gain understanding.

def validate_amount(amount_str):
    """
    Validate that amount is a float.
    """
    try:
        return float(amount_str)
    except ValueError:
        raise ValueError("Amount must be a number.")
'''