# budget.py
# Defines the Budget class for summaries and insights
# massage to Contributors: Implement methods to calculate totals and averages
'''
it is the sample code used for referance in future.
class Budget:
    def __init__(self, transactions):
        """
        Initialize with a list of Transaction objects.
        """
        self.transactions = transactions

    def total_income(self):
        return sum(t.amount for t in self.transactions if t.t_type == "income")

    def total_expense(self):
        return sum(t.amount for t in self.transactions if t.t_type == "expense")

    def balance(self):
        return self.total_income() - self.total_expense()

'''